export interface IInventor {
    code: string;
    firstName: string;
    lastName: string;
    gender: string;
    knownFor: string;
    dateOfBirth: string;

    // getInventorFullName(inventorFullName: string): string;
}